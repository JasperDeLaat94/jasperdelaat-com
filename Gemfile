source "https://rubygems.org"

# Jekyll and theme
gem "jekyll"
gem "minimal-mistakes-jekyll"

# Jekyll plugins
group :jekyll_plugins do
  gem 'jekyll-archives'
  gem 'jekyll-include-cache'
end

# Jekyll seems to depend on erb, but not install it
gem "erb"
